# -----------------------------------------------------------------------------
# 패키지 : log_simulator
# 목적   : 로그 시뮬레이터 최상위 패키지
# 구성   : core(엔진/조립), config(프로파일/유틸), simulator(생성기/파이프라인),
#          publisher(발행/DLQ), producer(Kafka 래퍼), main(엔트리)
# -----------------------------------------------------------------------------
