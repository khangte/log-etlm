# -----------------------------------------------------------------------------
# 패키지 : log_simulator
# 목적   : 로그 시뮬레이터 최상위 패키지
# 구성   : config(설정/유틸), simulator(도메인별 생성기),
#          pipeline_builder(파이프라인 조립), kafka_pipeline(발행 루프), producer(Kafka 래퍼), main(엔트리)
# -----------------------------------------------------------------------------
